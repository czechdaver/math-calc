/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Future i18n configuration will go here
  // i18n: {
  //   locales: ['cs', 'en', 'de'],
  //   defaultLocale: 'cs',
  // },
};

module.exports = nextConfig;
